@model List<Sistema_de_contabilidad_Open_Source.Models.ViewModels.ListcuentasAuxiliaresViewModel>
@{
    ViewBag.Title = "Cuentas auxiliares";
}

<h2>@ViewBag.Title</h2>


<div class="row" style="text-align: right; padding:5px;">
    <a href="~/cuentasAuxiliares/Nuevo" class="btn btn-primary"> Nuevo </a>
</div>
<div class="row">
    <div class="col-md-12">
        <table class="table">

            <tr>
                <th>Descripcion</th>
                <th>Estado</th>
            </tr>
            @foreach (var oElemento in Model)
            {
                <tr>
                    <td>@oElemento.descripcion</td>
                    @* Estado *@
                    @if (@oElemento.estado == true)
                    {
                        <td style="color:green"> Activado </td>
                    }
                    else
                    {
                        <td style="color:red"> Desactivado </td>
                    }
                    <td>
                        <a href="/cuentasAuxiliares/Editar/@oElemento.ID" class="btn btn-warning"> Editar</a>
                        <a href="/cuentasAuxiliares/Eliminar/@oElemento.ID" class="btn btn-danger"> Eliminar</a>
                    </td>
                </tr>
            }
        </table>

    </div>

</div>